{
  "name": "sample-cypress",
  "version": "1.0.0",
  "description": "Zebrunner Cypress reporting agent sample",
  "scripts": {
    "test": "npx cypress run --headed --spec cypress/e2e/basic.cy.js",
    "abort": "npx cypress run --headed --spec cypress/e2e/search.cy.js",
    "for_threads": "npx cypress run --headed --spec cypress/e2e/for_threads/*.cy.js"
  },
  "dependencies": {
    "@zebrunner/javascript-agent-cypress": "^1.2.1",
    "cypress-wait-until": "^1.7.2",
    "cypress-xpath": "^2.0.1",
    "standard": "^16.0.4"
  },
  "devDependencies": {
    "cypress": "^12.13.0"
  }
}

{
  "name": "sample-cypress",
  "version": "1.0.0",
  "description": "Zebrunner Cypress reporting agent(v9) sample",
  "scripts": {
    "test": "npx cypress run --headed --spec cypress/integration/basic.js",
    "abort": "npx cypress run --headed --spec cypress/integration/search.js",
    "for_threads": "npx cypress run --headed --spec cypress/integration/for_threads/*.js"
  },
  "dependencies": {
    "@zebrunner/javascript-agent-cypress": "^1.2.1",
    "cypress": "^9.7.0",
    "cypress-wait-until": "^1.7.2",
    "cypress-xpath": "^2.0.1",
    "standard": "^16.0.4"
  }
}
